```javascript
Array.from({length: n}, () => Array(m).fill(false));
```
- n x m 2차원 배열을 생성하는 코드

--- 

```javascript
parseInt()
```
- 문자열을 숫자로 변환해주는 함수

--- 

```javascript
answer.sort((a,b) => a - b);
```
- 숫자를 기준으로 오름차순 정렬해주는 경우