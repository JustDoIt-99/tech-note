```java
int left = Arrays.stream(num).max().getAsInt(); // num배열 최댓값 구하기
int right = Arrays.stream(num).sum(); // num배열 합계 구하기
```

```java
checkRoot.iterator().next(); // Set<Integer>의 첫번째 원소 꺼내기
```
