### 왜 HashMap을 사용했는가? 

패턴 문자열을 통해 숫자를 O(1)의 시간복잡도로 조회가 가능하기 때문입니다.  

### 왜 StringBuilder를 새로 생성했는가?

이전 패턴을 초기화하기 위해서 사용했습니다.  
객체를 새로 생성한다는 관점에서는 `sb.setLength(0)`을 사용해 객체를 재사용하는 점이 더 효율적으로 생각됩니다.  

### 왜 String을 key로 사용했나요?

빠르게 구현하기에는 String을 사용하는 것이 더 좋다고 판단하여 사용했습니다.

